# CAST AI Node Template: hyperscale_spot
apiVersion: apps/v1
kind: Deployment
metadata:
  name: monarch-prep
  namespace: default
  labels:
    app: monarch
    workload: prep
spec:
  replicas: 0
  selector:
    matchLabels:
      app: monarch-prep
  template:
    metadata:
      labels:
        app: monarch-prep
        spark-nodeselect-instance-type: amd64-64-16
        spark-nodeselect-nodepool-group: hyper
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  spark-nodeselect-nodepool-group: hyper
              topologyKey: kubernetes.io/hostname
      containers:
      - name: monarch-prep
        image: public.ecr.aws/docker/library/busybox:stable
        command: ["/bin/sh", "-c"]
        args:
          - |
            counter=0
            while true; do
              echo "Counter: $counter"
              counter=$((counter + 1))
              sleep 1
            done
        resources:
          requests:
            cpu: "1"
            memory: "2Gi"
            ephemeral-storage: "8Mi"
      nodeSelector:
        workload: monarch-prep
        spark-nodeselect-instance-type: amd64-64-16
        spark-nodeselect-nodepool-group: hyper
        spark-nodeselect-preemptible: "true"
      terminationGracePeriodSeconds: 10
