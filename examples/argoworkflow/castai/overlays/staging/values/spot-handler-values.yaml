# Staging spot handler configuration

clusterID: "staging-cluster-12345"

resources:
  requests:
    cpu: 20m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

# Run on all nodes
tolerations:
  - operator: Exists

priorityClassName: system-node-critical